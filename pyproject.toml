[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "labcraft"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "click>=8.0,<9",
    "rich>=12.0,<15"
]

[project.scripts]
labcraft = "labcraft.cli:cli"